import { Fira_Code, Inter, Lusitana, Poppins } from 'next/font/google';

export const fira_Code = Fira_Code({ weight: ['400', '700'] });
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '700' });
export const poppins = Poppins({ weight: ['400', '700'] });